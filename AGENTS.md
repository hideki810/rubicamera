# AGENTS.md — ルビカメラ 開発ガイド（AIエージェント共通）

このファイルは **Codex / Claude Code など、どのAIコーディングエージェントでも同じルールで作業できるようにするための指示書** です。
作業を始める前に必ず最後まで読んでください。人間の開発者が読んでも構いません。

---

## 1. このプロジェクトは何か

**ルビカメラ** — 紙の文書やお知らせをスマホのカメラで写すと、漢字にふりがな（ルビ）を振って読みやすくするWebアプリ。

- 想定ユーザー：漢字が苦手な人、外国人スタッフ、高齢者、子ども
- 想定端末：**スマートフォンのブラウザ（Android / iPhone）が主戦場**。PCは確認用
- 公開先：GitHub Pages（`main` ブランチ）→ https://hideki810.github.io/rubicamera/
- リポジトリ：https://github.com/hideki810/rubicamera （公開リポジトリ）

### 3つの画面（それぞれ独立した1枚のHTML）

| ファイル | 名前 | 何をするか | 現在の版 |
|---|---|---|---|
| `index.html` | ルビカメラ①（高精度版） | 写真を撮る／選ぶ → ふりがなを振る | `v6.4` |
| `live.html` | ルビカメラ②（かざして読む） | カメラ映像からリアルタイムでふりがな表示 | `v1.5` |
| `imi.html` | ルビカメラ③（意味しらべ） | ふりがな＋むずかしい言葉の意味を表示 | `v1.1` |

3画面はお互いに `<a href>` でリンクしており、**APIキーは3画面で共有**（後述）。

### 動作モード

- **オフラインモード**（`index.html` のみ）：APIキーなし。`tesseract.js`（OCR）＋ `kuromoji.js`（形態素解析）をCDNから読み込み、端末内だけで処理する。写真は外に出ない。
- **高精度モード / AIモード**：ユーザーが自分のGemini APIキーを入れると、Google Gemini API（`generativelanguage.googleapis.com/v1beta`）に画像を送って読み取る。`live.html` と `imi.html` はこちら専用。

---

## 2. 絶対に守るルール（これを破る変更はしない）

1. **ビルド工程を作らない。** npm / webpack / バンドラ / フレームワークを導入しない。
   各HTMLは **単体で開けば動く1ファイル完結**（HTML＋CSS＋JSを1枚に内包）のまま維持する。
   GitHub Pagesに置くだけで動く、というのがこのプロジェクトの生命線。
2. **APIキーをリポジトリに書かない。** キーはユーザーが自分で入力し、`localStorage` の
   キー名 `rubiKey` にその端末内だけで保存される。サンプルであっても実在のキーをコミットしない。
3. **サーバーを作らない。** バックエンド・プロキシ・環境変数は使わない。ブラウザから直接APIを叩く構成のまま。
4. **UIの日本語は「やさしい日本語」で。** 対象ユーザーは漢字が苦手な人。
   専門用語（トークン、エンドポイント、パース等）を画面に出さない。エラーも「何をすれば直るか」を書く。
5. **外部ライブラリを増やさない。** 追加が必要なら、まず理由を提案し、承認を得てから。
   現在の外部依存は Google Fonts / tesseract.js / kuromoji.js の3つだけ（すべてCDN）。
6. **既存の3ファイルを勝手に統合・分割しない。** 「共通部分をJSに切り出す」も上の1.に反するのでしない。

---

## 3. コードの書き方（既存コードに合わせる）

- **コメントは日本語。** 「なぜそうしたか」を書く。既存コードのコメント密度に合わせる。
- **CSSは `:root` のCSS変数**（`--ink` `--paper` `--accent` `--shu` `--line` `--radius` など）を使う。
  色を直書きせず、既存の変数を使い回す。
- **DOM取得は `const $ = id => document.getElementById(id)`** を使う（既に各ファイル先頭にある）。
- **localStorage は直接触らず `store.get/set/del` 経由。** プライベートブラウズ等で例外が出る端末があるため、
  `store` はメモリへのフォールバックを持っている。
- インデントは半角スペース2つ。セミコロンあり。既存の書き方から外れない。

### 3画面で共通しているコード（片方だけ直さない）

以下は3ファイルにコピーで存在する。**片方を直したら、他も同じように直す。**

- `store`（localStorageラッパー）
- `apiKey()` / `normalizeKey()` / `keyLooksValid()` — キーの取り出しと入力ゆれの自動補正
  （全角→半角、空白・不可視文字の除去、URLごと貼られた場合の `#k=` 抽出）
- `location.hash` の `#k=...` からキーを受け取って保存する即時関数
- `APP_VERSION` の表示、「最新版に更新する」ボタン（`?v=` を付けて再読み込み）
- モデル一覧の取得とフォールバック（`modelCache` / `gemini-flash-latest` 等）

---

## 4. 変更したら必ずやること

1. **`APP_VERSION` を上げる。** 直したファイルの `const APP_VERSION = '…'` を必ず更新する。
   - バグ修正・小さな改善 → 小数点以下を＋1（例 `v6.4` → `v6.5`）
   - 画面や機能の大きな変更 → 整数を＋1（例 `v6.5` → `v7.0`）
   - **理由**：スマホに古い版がキャッシュで残る事故が実際に起きており、ユーザーは
     画面下のバージョン表示と接続テストの1行目だけで「新しい版かどうか」を判断している。
     バージョンを上げ忘れると、直したのに直っていないように見える。
2. **3ファイルの共通部分を直したなら、3つとも直してバージョンを上げる。**
3. **動作確認する**（次章）。
4. **コミットメッセージは日本語**で、「何を直したか」＋「なぜ」を書く。既存の履歴の書き方に合わせる。

---

## 5. 動作確認のしかた

テストフレームワークもCIもない。手で確認する。

```bash
# リポジトリのルートで
python3 -m http.server 8000
# → http://localhost:8000/index.html をブラウザで開く
```

確認するポイント：

- **JSエラーが出ていないこと**（ブラウザの開発者ツール Console を必ず見る）
- `index.html`：写真を選んでふりがなが出るか。設定（歯車）→「接続テスト」が最後まで走るか
- `live.html`：カメラ許可 → 映像にふりがなが重なるか。**カメラは `https` かつ実機でないと動かない**ため、
  PCで確認できない部分は「未確認」と正直に報告する
- `imi.html`：ことばカードに意味が出るか
- 画面下のバージョン表示が、上げた `APP_VERSION` になっているか
- **スマホでの見た目**（開発者ツールのモバイル表示で幅375px前後を確認。本体は `max-width:560px`）

Gemini APIを実際に叩く確認には本物のAPIキーが要る。エージェントは自分でキーを用意しないこと。
APIの実挙動が絡む変更は、**コードの筋道を説明したうえで「実機での確認をお願いします」と伝える**。

---

## 6. Git の使い方

- 既定ブランチは `main`。**`main` に直接pushしない。** 作業用ブランチを切る。
  - Codex: `codex/内容がわかる名前`
  - Claude: `claude/内容がわかる名前`
- コミットメッセージは日本語。1行目に要約、空行、そのあとに変更点の箇条書きと理由。
- Pull Request は、依頼されたときだけ作る。
- **APIキー・個人情報が差分に混ざっていないか、コミット前に `git diff` で必ず目視確認する。**

コミットメッセージの例（既存の履歴より）：

```
v1.4: 「AIが読み取り中」のまま止まる問題を修正

・応答が途中で切れたときに待ち続けていたため、タイムアウトを入れた
・失敗しても画面に理由を出すようにした
```

---

## 7. 作業の進め方（エージェント向け）

- **既存の動きを壊さないことが最優先。** このアプリは実際に使われている。
- 頼まれていないリファクタリング・デザイン変更・機能追加をしない。差分は小さく。
- 迷ったら、勝手に決めずに聞く。ただし調べればわかることは自分で調べる。
- 作業が終わったら、**日本語で** 次を報告する：
  1. 何を直したか
  2. どのファイルの `APP_VERSION` をいくつにしたか
  3. 自分で確認できたこと／確認できていないこと（実機確認が必要な点）

---

## 8. 用語メモ

- **ルビ / ふりがな**：漢字の上に小さく振るかな。HTMLでは `<ruby>漢字<rt>かんじ</rt></ruby>`
- **オフラインモード**：APIキーなしで端末内だけで処理するモード（`index.html` のみ）
- **高精度モード**：Gemini APIを使うモード
- **`rubiKey`**：APIキーを入れる localStorage のキー名。3画面共通
