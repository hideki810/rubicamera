# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業するときの指示書です。

## プロジェクト概要

「ルビカメラ」は、写真やカメラ映像に写った日本語の文章を読み取り、漢字にふりがな（ルビ）を付けて表示するスマホ向けの Web アプリです。
漢字が読みづらい人（子ども・高齢者・日本語学習者など）が使うことを想定しています。

- ビルド不要の**素の HTML / CSS / JavaScript**。フレームワークもパッケージマネージャも使っていません。
- 各画面は HTML・CSS・JS を 1 枚にまとめた作りで、3 画面で共通の処理だけを `common.js` に切り出しています。
- 静的ファイルをそのまま配信するだけで動きます（サーバー側の処理はありません）。
- 文字認識・ルビ付けは **Google Gemini API** をブラウザから直接呼び出して行います。

## ファイル構成

| ファイル | 画面名 | 役割 |
|---|---|---|
| `common.js` | （共通処理） | 3画面が共有する処理。APIキー、モデル選び、エラー判定、画像のbase64化など |
| `index.html` | ルビカメラ（高精度版） | 写真を撮る／選ぶ → 全文にルビを付けて表示。オフラインモード（Tesseract + kuromoji）も持つ唯一の画面 |
| `live.html` | ルビカメラ２（かざして読む） | カメラをかざすと、映像に重ねてリアルタイムでルビを表示 |
| `imi.html` | ルビカメラ３（意味しらべ） | 読み取った文章から難しい言葉を選び、意味をやさしく説明 |
| `icon-1/2/3.svg` `icon-*-192.png` `icon-*-512.png` | （アイコン） | 画面ごとのアイコン。SVGが原本で、PNGはそこから書き出したもの |
| `manifest-1/2/3.json` | （アプリ設定） | ホーム画面に「アプリとして」追加するための設定 |
| `sw.js` | （サービスワーカー） | ホーム画面追加を可能にするためだけの最小構成。**何もキャッシュしない** |

`index.html` が入口で、そこから `live.html` / `imi.html` へ移動できます。`live.html` と `imi.html` からは `index.html` に戻れます。

## 共通処理は `common.js` にまとめてある

3画面で同じだった処理は `common.js` に切り出してあります。**共通の挙動を直すときは `common.js` を 1 か所直すだけで 3 画面に反映されます。**

`common.js` にあるもの：

- `$()`、`sleep()`、`store`（localStorage が使えない環境では `mem` に退避）
- APIキー関連：`apiKey()` / `normalizeKey()` / `keyLooksValid()`
- `setupVersionUI(APP_VERSION)` … バージョン表示と「最新版に更新する」ボタンの設定
- `isTransient()` … 429・5xx・混雑メッセージを「再試行する価値あり」と判定
- `getModelCandidates(options)` と `STABLE_BACKUPS` / `STABLE_BACKUPS_LITE` … 使う Gemini モデルの決定
  - `live.html` だけは速さ優先なので `getModelCandidates({preferLite: true})` と呼ぶ（同じ世代なら軽量版を先に使う）
- `setupPageNav(自分のファイル名)` と `PAGES` … 3画面を行き来する切り替えバー
  - 各HTMLの `<main>` の先頭に置いた `<nav id="pageNav"></nav>` の中に、3画面へのボタンを作る
  - 今いる画面は色を反転させ、リンクにしない
  - ボタンの文字や並び順を変えるときは `PAGES` を直せば3画面に反映される
- `KANJI_RE` … 漢字を含むかの判定
- `toBase64()` / `canvasBase64()` / `fileToBase64()` … 写真を AI に送れる形にする

各 HTML の中に残っているもの（その画面だけの処理）：

- `index.html` … オフラインモード（Tesseract + kuromoji）、画像の二値化、ルビの組み立て
- `live.html` … カメラ制御、動き検出、映像へのルビ重ね表示
- `imi.html` … ことばカード、意味の問い合わせ、単語の選択

設定画面・ヘッダー・フッターの **見た目（CSS）は今も 3 ファイルにコピーされています**。デザインを変えるときは 3 ファイルとも直してください（切り替えバーの CSS だけは `common.js` の中にあります）。

## アイコンとホーム画面への追加

3画面はそれぞれ別のアプリとしてホーム画面に追加できます。アイコンは「ふりがな付きの漢字1文字」で統一し、色で見分けます。

| 画面 | アイコン | 色 |
|---|---|---|
| `index.html` | じ／字 | 濃紺 `#1b2a41` |
| `live.html` | み／見 | 青 `#2b6cb0` |
| `imi.html` | い／意 | 緑 `#2f7a5b` |

- **アイコンを変えるときは `icon-N.svg` を直し、そこから PNG（192px と 512px）を作り直してください。** PNG だけ直すと SVG とズレます。
- Android のホーム画面ではアイコンが円などに切り抜かれます（maskable）。文字は中央 80% の内側に収めてください。
- `manifest-N.json` の `id` と `start_url` が画面ごとに違うことで、3つが別アプリとして扱われます。ここを同じにしないでください。

### `sw.js` に機能を足さないこと

`sw.js` は**ホーム画面に「アプリとして」追加できるようにするためだけ**に置いています。ファイルを一切キャッシュしません。

**ここにキャッシュ機能を足さないでください。** 以前あった「古い画面が端末に残って直したはずの不具合が消えない」という問題が、もっと厄介な形でぶり返します（サービスワーカーのキャッシュは、利用者が自分で消すのがとても難しいためです）。

## 守ってほしいルール

### バージョン表記
- 画面を変更したら、そのファイルの `APP_VERSION` を必ず上げてください（例：`v6.5` → `v6.6`）。
- 利用者の端末に古い版が残っていないか確認するための番号なので、修正したのに据え置くのは NG です。
- 3 ファイルのバージョン番号は**それぞれ独立**です（揃える必要はありません）。

### APIキー
- Gemini の APIキーは利用者が設定画面で入力し、`localStorage` のキー名 **`rubiKey`** に保存します。3 画面で共有されます。
- **コード内にAPIキーを書き込まない。コミットもしない。**
- キーの形式チェックは意図的に緩くしています（`keyLooksValid()`）。Google の仕様変更で弾かれる事故を避けるため、厳しくしないでください。

### Gemini モデルの選び方
- モデル名を 1 つに固定しないでください。`common.js` の `getModelCandidates()` が Google のモデル一覧 API から「新しい flash 系」を自動で選び、取得に失敗したときだけ固定の候補（`fallback` / `STABLE_BACKUPS`）を使う作りです。
- `preview` `exp` `thinking` `image` `tts` `audio` `live` `8b` を含むモデルは除外しています。この判定は `common.js` の 1 か所にあります。
- 応答を速くするため、`gemini-3` 系は `thinkingLevel: 'low'`、`gemini-2.5` 系は `thinkingBudget: 0` を指定し、400 が返ったら指定なしで送り直しています（`live.html` 内）。

### エラー処理
- 無料枠の回数制限（429）や混雑（503）で止まらないよう、**複数モデルへの順次フォールバック＋待機してリトライ**が基本方針です。この粘り強さを削らないでください。
- 再挑戦するかの判定（`isTransient()`）は `common.js`、実際のリトライ処理は画面ごとに各 HTML の中にあります。
- エラー文は利用者に向けたやさしい日本語で。技術的なエラーコードをそのまま出さないこと。

### UI と文言
- 対象は「漢字が苦手な人」です。**やさしい日本語**で、専門用語を避けてください。
- 画面は**スマホ縦持ち前提**（`max-width:560px`）。ボタンは指で押しやすい大きさを保つこと。
- 色・角丸は `:root` の CSS 変数を使い、値を直書きしないでください。
- コード中のコメントは**日本語**で書いてください（既存のコメントに合わせる）。

### キャッシュ対策
- 利用者の端末に古い版が残る問題があったため、各画面に「最新版に更新する（古い画面のまま動くとき）」ボタン（`btnUpdate`）を置き、`?v=<時刻>` を付けて読み込み直しています。この仕組みは 3 画面すべてに必要です。
- **`common.js` を直したときは、3 つの HTML にある `<script src="common.js?v=3">` の数字を 3 ファイル同時に上げてください**（`?v=4`、`?v=5` …）。ここを上げないと、利用者の端末に古い `common.js` が残ったままになります。
- この番号は各画面の `APP_VERSION` とは別物です。混同しないでください。

## 動作確認

ビルド・テスト・リンタはありません。確認は次の手順で行います。

```bash
python3 -m http.server 8000   # リポジトリ直下で実行し、http://localhost:8000/ を開く
```

- `file://` で直接開くとカメラや一部機能が動きません。また `common.js` の読み込みも失敗することがあります。必ず HTTP サーバー経由で確認してください。
- **カメラ機能（`live.html`、写真撮影）は実機での確認が必要**です。ブラウザのカメラ許可は HTTPS または localhost でのみ得られます。
- Gemini の呼び出しには利用者自身のAPIキーが要ります。手元にキーがない場合は、コードを読んでの確認にとどめ、「実機での動作は未確認」と正直に伝えてください。
- `index.html` の設定画面には「接続テスト」機能があります。APIキーまわりを触ったときはここで確認できます。

## 外部依存（CDN）

- `tesseract.js@5`、`kuromoji@0.1.2`（＋辞書）… `index.html` のオフラインモードのみ
- Google Fonts（Zen Maru Gothic / Noto Sans JP）
- Google Gemini API（`generativelanguage.googleapis.com/v1beta`）

npm パッケージの追加やビルドツールの導入は、利用者から明示的に頼まれない限り行わないでください。「HTML を 1 枚置けば動く」ことがこのアプリの利点です。

## Git 運用

- コミットメッセージは**日本語**で、何をどう直したかが利用者にも分かるように書いてください（例：`v1.4: 「AIが読み取り中」のまま止まる問題を修正`）。
- バージョンを上げた修正は、コミットメッセージの先頭にそのバージョンを書く慣習です。
- 指定された作業ブランチに push します。プルリクエストは、明示的に頼まれたときだけ作成してください。
